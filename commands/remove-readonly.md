---
description: Remove a folder from the read-only list
argument-hint: <path> [--global]
allowed-tools: Bash(node:*)
---

Run this command and show the output to the user:

!`node ~/.claude/commands/remove-readonly.js $ARGUMENTS`
